# VistterStream Environment Example
# Copy to .env and adjust values for your deployment

# Database configuration
DATABASE_URL=sqlite:////data/vistterstream.db

# Uploads directory inside container
UPLOADS_DIR=/data/uploads

# RTMP relay service name (for Docker networking)
RTMP_RELAY_HOST=rtmp-relay
RTMP_RELAY_PORT=1935

# CORS origins (comma-separated)
CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:5173

# PTZ / ONVIF configuration
# Enable verbose PTZ logging (set to 1 for debug runs)
PTZ_DEBUG=0
# Explicit ONVIF device service URL to bypass discovery (example Sunba PTZ camera)
ONVIF_DEVICE_URL=http://192.168.12.59:8899/onvif/device_service
# Optional explicit PTZ service URL override; leave empty to use device capabilities
ONVIF_PTZ_URL=
